К сожалению, на данный момент нет решений по чтению текстового файла целиком в переменную с разделителями строки на подобии "\0 && \n", поэтому придётся заранее подготавливать файлы спама.
А именно удалять в них разделение, приводя сообщение в одну длинну строку, чтобы ф-ция на подобии getline() или get() могла нормально кушать файл.

Решение предыдущей проблемы было тривиальным, программа learning.cpp выдаёт файл-модель. Следующим шагом является написание программы определителя.
